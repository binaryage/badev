///
/// Specific configuration for TotalTerminal shell
///

GCC_PREFIX_HEADER = TotalTerminalShared.pch
INFOPLIST_FILE = Info.plist
PRODUCT_NAME = TotalTerminal

<% $shared["HEADER_SEARCH_PATHS"] << "$(SRCROOT)/../headers" %>
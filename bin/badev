#!/usr/bin/env ruby
# coding: utf-8

# resolve bin path, ignoring symlinks
require "pathname"
bin_dir = File.dirname(Pathname.new(__FILE__).realpath)

# add self to libpath
$:.unshift File.expand_path("../lib", bin_dir)

require "badev/cli"
Badev::CLI.start(*ARGV)